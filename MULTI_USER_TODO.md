# GA 多用户与后台管理 TODO

目标：把当前单用户、本地工作区模型，升级为支持约 20 人同时使用的服务端版本，并提供管理员后台查看/复制用户文件的能力。

## 阶段 1：账号与目录隔离

- [x] 明确当前限制：`state.agent` 为全局单例，不适合直接承载多用户并发
- [x] 引入用户表、会话表、密码散列存储
- [x] 提供登录、登出、当前用户接口
- [x] 引入管理员角色
- [x] 将用户数据目录调整为 `workspace/users/<username>/ga_config/`
- [x] 把 `mykey`、`ToDo`、`memory`、`sche_tasks` 的读写切换到用户目录

## 阶段 2：后台管理能力

- [x] 提供管理员用户列表接口
- [x] 提供管理员查看任意用户文件树接口
- [x] 提供管理员读取任意用户文件内容接口
- [x] 提供管理员跨用户复制文件接口
- [ ] 补充文件操作审计字段

## 阶段 3：前端登录与后台界面

- [ ] 增加登录页/登录态恢复
- [ ] 普通用户仅可见自己的配置与文件
- [ ] 管理员增加后台管理页面
- [ ] 管理员可在界面中浏览所有用户文件并执行复制

## 阶段 4：真正的多用户运行态

- [x] 将 `state.agent` 从全局单例拆为按用户隔离
- [x] 将 `/api/chat`、`/api/stream`、`/api/status`、`/api/control` 切到用户会话上下文
- [ ] 处理每个用户独立的历史、停止控制、调度器和自动模式
- [ ] 验证约 20 个用户并发情况下的资源占用与稳定性

## 本轮执行范围

- [x] 产出多用户改造 TODO
- [x] 先完成阶段 1 的后端基础设施
- [x] 先完成阶段 2 的后端 API
- [ ] 前端后台界面后续继续接入
